# Discord Webhook URL
# Get this from Discord: Server Settings > Integrations > Webhooks > New Webhook
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Maximum number of upcoming events to show (default: 5)
MAX_UPCOMING=5

# Minimum impact level to filter events (options: all, high, medium, low)
# all = show all events
# high = only high impact events
# medium = medium and high impact events
# low = low, medium and high impact events
MIN_IMPACT=all

# Comma-separated list of currency pairs to filter (leave empty for all currencies)
# Examples: USD,EUR,GBP or USD or leave empty for all
CURRENCIES=

# Timezone for scheduling and date grouping (default: Asia/Jakarta)
# Examples: Asia/Jakarta, America/New_York, Europe/London, UTC
# Full list: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TIMEZONE=Asia/Jakarta

# Discord webhook username (default: Forex Notifier)
WEBHOOK_USERNAME=Forex Notifier

# Embed title prefix (default: Forex Alerts)
EMBED_TITLE=Forex Alerts

# Server/footer name (default: Forex News)
SERVER_NAME=Forex News

# ==================== ADVANCED SETTINGS ====================
# Most users won't need to change these settings below

# Schedule: Hour to run daily notification (0-23, default: 7 for 7 AM)
SCHEDULE_HOUR=7

# Schedule: Minute to run daily notification (0-59, default: 0)
SCHEDULE_MINUTE=0

# Maximum number of retry attempts for failed HTTP requests (default: 3)
MAX_RETRIES=3

# Initial sleep time in seconds before fetching data (default: 5)
INITIAL_SLEEP_SECONDS=5

# HTTP request timeout in seconds (default: 10)
REQUEST_TIMEOUT=10

# Discord embed color in hex format without 0x (default: FF4500 for orange-red)
# Examples: FF4500 (orange-red), 5865F2 (blurple), 57F287 (green), ED4245 (red)
EMBED_COLOR=FF4500

# Maximum characters for event titles before truncation (default: 30)
MAX_EVENT_TITLE_LENGTH=30

# User-Agent header for HTTP requests (optional, uses default Chrome user-agent if not set)
# USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/91.0.4472.124 Safari/537.36
